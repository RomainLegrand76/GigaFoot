<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Inscription{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
    <script src="{{ asset('build/app.js') }}"></script>
    <link rel="stylesheet" href="{{ asset('build/app.css') }}">
</head>

{% block body %}
    <body id="bodyConnect">
    <main id="mainInscription">
        <div class="row">
            <div class="offset-lg-5	 col-lg-2" id="containerConnect">
                <div  id="divConnect">
                    <img src="images/Logo-2.png">
                </div>
                {{ form_start(inscription) }}
                {{ form_widget(inscription) }}
                {{ form_end(inscription) }}
                <br>
                <a href="/connexion"><input  type="button" name="bouttonAnnuler"
                                             class="btn btn-warning" id="bouttonAnnuler"
                                             value="Annuler" ></a>
            </div>
        </div>
    </main>
    </body>
{% endblock %}
